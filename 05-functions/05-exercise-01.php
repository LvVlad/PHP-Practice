<?php

//Create a function that accepts any string and returns the same value with added "codelex" by the end of it. Print this value out.

$stringTest = 'Macos izmeginat';
$stringInput = readline('Write something here:');

function addBrand($string) {
    echo "$string by codelex.";
}

//addBrand($stringTest);
addBrand($stringInput);